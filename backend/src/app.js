'use strict';
const express=require('express');const cors=require('cors');
const {frontendOrigin}=require('./config/env');
const authRoutes=require('./routes/auth');const scoreRoutes=require('./routes/scores');const charityRoutes=require('./routes/charities');const adminCharityRoutes=require('./routes/adminCharities');const subscriptionRoutes=require('./routes/subscriptions');const adminDrawRoutes=require('./routes/adminDraws');const publicDrawRoutes=require('./routes/draws');const winnerRoutes=require('./routes/winners');const adminWinnerRoutes=require('./routes/adminWinners');const adminRoutes=require('./routes/admin');const webhookRoutes=require('./routes/webhooks');const {errorHandler}=require('./middleware/errorHandler');
function allowedOrigin(origin){if(!origin)return true;const env=process.env.NODE_ENV||'development';const prod=frontendOrigin();if(env==='production')return origin===prod;return origin===prod||origin==='http://localhost:5173'||origin==='http://127.0.0.1:5173'||origin==='http://localhost:4173'||origin==='http://127.0.0.1:4173';}
function createApp(){const app=express();app.set('trust proxy',1);app.use(cors({origin:(origin,cb)=>{if(allowedOrigin(origin))return cb(null,true);return cb(new Error('Origin not allowed by CORS'));},credentials:true}));app.use('/api/webhooks',webhookRoutes);app.use(express.json({limit:'1mb'}));app.get('/health',(req,res)=>res.json({ok:true}));app.use('/api/auth',authRoutes);app.use('/api/scores',scoreRoutes);app.use('/api/charities',charityRoutes);app.use('/api/admin/charities',adminCharityRoutes);app.use('/api/subscriptions',subscriptionRoutes);app.use('/api/draws',publicDrawRoutes);app.use('/api/admin/draws',adminDrawRoutes);app.use('/api/winners',winnerRoutes);app.use('/api/admin/winners',adminWinnerRoutes);app.use('/api/admin',adminRoutes);app.use(errorHandler);return app;}
module.exports={createApp};
