'use strict';
const express=require('express');const c=require('../controllers/adminController');const {requireAuth,requireRole}=require('../middleware/auth');const r=express.Router();r.use(requireAuth,requireRole('admin'));r.get('/users',c.listUsers);r.get('/users/:id',c.getUser);r.put('/users/:id',c.updateUser);r.put('/users/:id/role',c.setRole);r.get('/users/:id/scores',c.listUserScores);r.put('/users/:id/scores/:scoreId',c.updateUserScore);r.delete('/users/:id/scores/:scoreId',c.deleteUserScore);r.get('/subscriptions',c.subscriptions);r.put('/users/:id/cancel-subscription',c.cancelSubscription);r.get('/reports',c.reports);r.get('/config',c.config);r.put('/config/pool-contribution',c.updateConfig);module.exports=r;
