'use strict';
const express=require('express');const c=require('../controllers/adminCharityController');const {requireAuth,requireRole}=require('../middleware/auth');const router=express.Router();router.use(requireAuth,requireRole('admin'));router.get('/:id',c.get);router.get('/:id/events',c.events);router.post('/:id/events',c.createEvent);router.put('/:id/events/:eventId',c.updateEvent);router.delete('/:id/events/:eventId',c.removeEvent);router.get('/:id/media',c.media);router.post('/:id/media',c.createMedia);router.delete('/:id/media/:mediaId',c.removeMedia);module.exports=router;
